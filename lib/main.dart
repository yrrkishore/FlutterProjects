import 'package:flutter/material.dart';
import 'package:hello_world_flutter/gridview.dart';

import 'barcodeEstimator.dart';

void main() {
  //runApp(MaterialApp(title: 'Barcode Estimator', home: BarcodeCalculator()));
  runApp(MaterialApp(title: 'Barcode Estimator', home: GridViewApp()));
}
